# yaml-language-server: $schema=../../schema/_template.json
commands:
  - yarn set version stable
  - yarn config set nodeLinker node-modules
  - git init

ignore:
  - node_modules
  - "*.lock"
  - "*.log"
  - "dist"
